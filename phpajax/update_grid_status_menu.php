<?php

//	extract($_REQUEST);

	print_r($_REQUEST);

	exit();
	
	
	
/*	
	$filename = "newfile.txt";


	$myWriteFile = fopen($filename, "w") or die("Unable to open file!");
	$txt = $selectedvalue;
	fwrite($myWriteFile, $txt);
	fclose($myWriteFile);
	
	echo $selectedvalue;
	
	
	$myReadFile = fopen($filename, "r") or die("Unable to open file!");
	echo fread($myReadFile,filesize($filename));
	fclose($myReadFile);
*/	
	
?>